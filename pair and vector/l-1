✅ Step-by-Step Procedure (Write + Perform in Lab)
🔹 Step 1: Configure your Git (only once)

This sets your name and email for commits.

git config --global user.name "Your Name"
git config --global user.email "your-email@example.com"


✅ Check if it’s set:

git config --list

🔹 Step 2: Create a folder and initialize a local repository
mkdir labproject
cd labproject
git init


🧩 Explanation:

mkdir labproject → creates a folder.

cd labproject → opens it.

git init → starts a new local repository (creates a .git folder).

🔹 Step 3: Check the repository status
git status


🧩 Shows whether any files are untracked, modified, or ready to commit.

🔹 Step 4: Create a new file
echo "Hello Git" > index.txt


Now check again:

git status


You’ll see index.txt as untracked.

🔹 Step 5: Add the file to staging area
git add index.txt


✅ This prepares it for commit.

Check again:

git status


Now it’ll show changes to be committed.

🔹 Step 6: Commit the file
git commit -m "Initial commit"


🧩 This saves your snapshot permanently in Git history.

🔹 Step 7: View commit history
git log


🧩 Displays commit IDs, author, date, and messages.

🔹 Step 8: Create a remote repository on GitHub

Go to https://github.com/new

Give it a name like labproject

Leave it empty (no README)

Click Create repository

Copy the HTTPS link (example):

https://github.com/your-username/labproject.git

🔹 Step 9: Link local repo to remote repo
git remote add origin https://github.com/your-username/labproject.git


Check:

git remote -v

🔹 Step 10: Push local commits to GitHub
git branch -M main
git push -u origin main


Now your project appears on GitHub 🎉

🔹 Step 11: Clone a repository (to simulate downloading)

Go back to your main directory:

cd ..
git clone https://github.com/your-username/labproject.git


🧩 This downloads your GitHub project to your computer.

🔹 Step 12: Try reset (to undo a commit or stage)

Add a line to file:

echo "Another line" >> index.txt
git add index.txt
git status


Now undo it:

git reset index.txt


🧩 This removes the file from staging area (unstages it).

If you want to undo the last commit:

git reset --soft HEAD~1


or completely remove it:

git reset --hard HEAD~1

🧾 Output Verification
Command	Expected Output
git status	shows file states
git log	shows commit history
git remote -v	shows GitHub URL
git clone	creates new local copy
git push	uploads to GitHub
git reset	unstages/undoes changes
🧠 Viva Questions (likely asked)

What does git init do?
→ Initializes a new local Git repository.

What is git add used for?
→ Adds changes to staging area.

What is git commit?
→ Saves the staged changes permanently in Git history.

What is the difference between git clone and git init?
→ git init creates a new repo, git clone copies an existing one.

What does git reset do?
→ Undoes staging or previous commits.

How do you connect a local repo to GitHub?
→ Using git remote add origin <URL>.

What’s the use of git config?
→ Sets user details like name and email.
